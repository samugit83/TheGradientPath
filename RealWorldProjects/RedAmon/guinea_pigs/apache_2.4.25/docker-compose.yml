version: '3.8'

services:
  vulnerable-apache:
    build: .
    container_name: vulnerable-apache-2.4.25
    ports:
      - "8080:80"
    restart: unless-stopped
    # Run as root to allow path traversal to system files
    # This is intentionally insecure for testing purposes
